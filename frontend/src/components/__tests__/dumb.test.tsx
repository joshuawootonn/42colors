import warning from '../warning';

describe('this is a test for the ci pipeline', () => {
    it('WHEN this test is run THEN it will pass', () => {
        expect(1).toBe(1);
        expect(1).toBe(1);
    });
});
